server:
  port: 9000

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/shahabu6796/Microservices-configuration
          clone-on-start: true